import Link from "next/link";
export type LawyerCardData={slug:string;nombre:string;apellidos:string;titulo?:string|null;ciudad:string;verificado?:boolean;ratingAvg?:number;ratingCount?:number;especialidades?:{nombre:string}[]};
export function AbogadoCard({lawyer}:{lawyer:LawyerCardData}){return <article className="rounded-xl border bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-4"><div><h2 className="text-lg font-semibold">{lawyer.nombre} {lawyer.apellidos}</h2><p className="text-sm text-slate-500">{lawyer.titulo ?? "Abogado"} · {lawyer.ciudad}</p></div>{lawyer.verificado&&<span className="text-xs font-semibold text-green-700">Verificado</span>}</div><p className="mt-3 text-sm">★ {(lawyer.ratingAvg??0).toFixed(1)} ({lawyer.ratingCount??0})</p><div className="mt-3 flex flex-wrap gap-2">{(lawyer.especialidades??[]).map(s=><span key={s.nombre} className="rounded-full bg-slate-100 px-2 py-1 text-xs">{s.nombre}</span>)}</div><Link className="mt-4 inline-block underline" href={"/abogado/"+lawyer.slug}>Ver perfil</Link></article>}